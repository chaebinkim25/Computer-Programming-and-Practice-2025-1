## 반복 심화

- `break` 문장의 적절한 쓰임새, 문법, 동작 방식은?

- `continue` 문장의 적절한 쓰임새, 문법, 동작 방식은?

- 숫자 `m`을 암호화하기 위해 `e`번 거듭제곱한 후 `n`으로 나눈 나머지를 구하는
  방법은?

- 숫자 `m`을 암호화하기 위해 `e`번 거듭제곱한 후 `n`으로 나눈 나머지를 구하는
  코드는?

- 곱한 후 `n`으로 나눈 나머지를 구하는 것을 더하기의 반복으로 구현하는
  방법은?

- 곱한 후 `n`으로 나눈 나머지를 구하는 것을 더하기의 반복으로 구현하는
  코드는?

- `m`번 더하기를 하는 것을 더 빠르게 할 수 있는 방법은?

- `m`번 곱하기를 하는 것을 더 빠르게 할 수 있는 방법은?

