## 반복 문법

- 반복해서 실행되는 문장을 적는 문법은?

- 다음 프로그램을 실행했을 때, 실행되는 순서는?

```c
int main(void)
{
        int i, state = 0;
        for (i = 0; i \< 3; i = i + 1) {
                state = i;
        }
        return 0;
}
```

- 변수에 저장된 비밀번호를 반복문으로 가능한 모든 숫자를 대입해서 찾는
방법은?

- 변수에 저장된 비밀번호를 반복문으로 가능한 모든 숫자를 대입해서 찾는
방법을 코드로 쓰면?

- 변수에 저장된 값과 다른 값을 가지고 연산을 한 결과를 변수에 다시
저장하는 연산자는?

- 0부터 15까지 십진수를 이진수로 바꾸는 방법과 코드는?

- 10번째 피보나치 수를 구하는 방법과 코드는?

- 이중 `for` 문장으로 0번부터 100번까지 유저 번호에 대해 0부터 9999까지
비밀번호를 확인하는 코드는?

