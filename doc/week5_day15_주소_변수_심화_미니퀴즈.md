## 주소 변수 심화

- 주소에서 더하기를 하는 것의 의미는?

- 인덱스 연산자 `[]`를 주소 찾아가기 연산자 `*`와 더하기 `+`의 조합으로
나타내면?

- 주소 변수를 루프 변수로 써서 반복하는 방법은?

- 인덱스 연산자 `[]`의 우선순위를 다른 연산자들과 비교하면?

- 주소 연산자 `&`의 우선순위를 다른 연산자들과 비교하면?

- 주소 찾아가기 연산자 `*`의 우선순위를 다른 연산자들과 비교하면?

