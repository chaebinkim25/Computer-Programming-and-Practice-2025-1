## 컴프실 6주차 16일차: 코드 묶음

---

### 복습: 코드 묶음 만들기

```c
int main(void)
{
	return 0;
}
```

코드 묶음 만들기:  

```c
타입    이름    (    입력정보    )    {    내용    }
```

반환 문장:  

```c
return    수식    ;
```

---

### 복습: 변수 만들기

```c
int main(void)
{
	int x = 0;
	return 0;
}
```

변수 만들기:  

```c
타입    이름    =    수식    ;
```

---

### 복습: 수식 문장

```c
int main(void)
{
	int x = 0;
	x = 1 + 1;
	return 0;
}
```

수식 문장:  

```c
수식    ;
```

---

### 복습: 복합 문장

```c
int main(void)
{
	int result = -1;
	{
		result = 1 + 1;
	}
	return 0;
}
```

복합 문장:  

```c
{    문장_여러개_또는_0개    }  
```

---

### 복습: 조건부 실행

```c
int main(void)
{
	int condition = 1, result = -1;
	if (condition) {
		result = 1 + 1;
	}
	return 0;
}
```

조건부 실행 문장:  

```c
if    (    수식    )    문장
```

---

### 복습: 반복 실행

```c
int main(void)
{
	int i, result = 0;
	for (i = 1; i <= 10; i = i + 1) {
		result = result + i;
	}
	return 0;
}
```

반복 실행 문장:  

```c
for    (    수식    ;    수식    ;    수식    )    문장
```

---

### 복습: 변수 묶음

```c
int main(void)
{
	int arr[5] = {10, 20, 30, 40, 50};
	return 0;
}
```

변수 묶음 만들기:  
```c
타입    이름    [    숫자    ]    =    {    값리스트    }    ;
```

---

### 복습: 주소

```c
int main(void)
{
	int x;
	int *ptr = &x;
	return 0;
}
```

주소 변수 만들기:  

```c
타입    *    이름    =    수식    ;
```

---

### 변수 묶음 vs 코드 묶음

변수 묶음과 비교해서 코드 묶음의 특징은 무엇일까? 

---

### 변수 묶음 vs 코드 묶음: 메모리 관점

변수 묶음은 메모리의 어디에 언제 어떻게 저장되고, 코드 묶음은 메모리의 어디에 언제 어떻게 저장될까?

---

### 변수 묶음 vs 코드 묶음: 만드는 시간 관점

블록 안에서 코드 묶음을 만드는 것을 허용하고 문법적으로 지원해주면 어떨까?

---

### 변수 묶음 vs 코드 묶음: 만드는 시간 관점

블록 밖에서 변수나 변수 묶음을 만드는 것을 문법적으로 지원해주는 것은 어떨까?

---

### 블록 밖에 만든 변수, 변수 묶음, 코드 묶음

```c
int x;
int arr[10];
int main(void)
{
	return 0;
}
```

소스코드:

- ```c
  변수_만들기
  ```

- ```c
  함수_만들기
  ```

---

### 블록 밖의 변수 묶음 vs 코드 묶음: 차이점

블록 바깥에 만든 변수 묶음과  코드 묶음이 서로 다른 점은 어떤 것이 있을까?

---

### 블록 밖의 변수 묶음 vs 코드 묶음: 공통점

블록 바깥에 만든 변수 묶음과 코드 묶음 간에 비슷한 점은 어떤 것들이 있을까?

---

### 변수 묶음에서 데이터를 얻는 방법은?

변수 묶음에서 데이터를 얻는 문법을 변수 묶음을 만드는 문법과 비교해보면?

---

### 코드 묶음에서 데이터를 얻는 방법은?

코드 묶음에서 데이터를 얻는 문법은 코드 묶음을 만드는 문법과 비교해서 어떻게 정하면 좋을까?

---

### 코드 묶음에서 데이터를 얻는 문법

```c
int zero(void) { return 0; }
int main(void)
{
	int x = zero();
	return 0;
}
```

함수 호출 수식:    

```c
이름    (    입력값정보    )
```

---

### 코드 묶음의 입력 값이 유효한 범위는?

코드 묶음에서 입력 값은 어느 범위에서 유효할까?


---

### 코드 묶음의 입력 값을 저장할 곳은?

코드 묶음의 입력 값을 저장할 곳은 어디에다 만드는 것이 적절할까?

---

### 코드 묶음의 입력 값을 저장할 곳 만들기

코드 묶음의 입력 값을 저장할 공간을 만들기 위해서 어떤 정보가 필요할까?

---

### 입력 값을 하나 받는 코드 묶음 만들기

입력 값을 하나 받는 코드 묶음을 만드는 코드는? 

---

### 입력 값을 하나 받는 코드 묶음 만들기

입력 값을 하나 받는 코드 묶음을 만드는 코드는? 

```c
int add_one(int val) 
{ 
	return val + 1; 
}
```

---

### 입력 값이 있는 코드 묶음 실행하기

입력 값이 있는 코드 묶음을 실행하려면 무슨 정보가 필요할까?

---

### 코드 묶음을 실행할 때 입력 값의 위치

입력 값이 있는 코드 묶음을 실행할 때, 입력 값은 어디에 적으면 될까?

---

### 입력 값을 하나 받는 코드 묶음 실행하기

입력 값을 하나 받는 코드 묶음을 실행하는 코드는?

---

### 입력 값을 하나 받는 코드 묶음 실행하기

입력 값을 하나 받는 코드 묶음을 실행하는 코드는?

```c
int add_one(int val) { return val + 1; }
int main(void) 
{
	int x = add_one(5);
	return 0;
}
```

---

### 코드 묶음에 입력 값을 두개 쓰려면?

코드 묶음에서 입력 값이 두개라면, 두 입력 값을 무엇으로 구분하면 좋을까?

---

### 입력 값이 두개인 코드 묶음 만들기

입력 값이 두개인 코드 묶음을 만드는 코드는?

---

### 입력 값이 두개인 코드 묶음 만들기

입력 값이 두개인 코드 묶음을 만드는 코드는?

```c
int add(int x, int y) 
{ 
	return x + y; 
}
```

---

### 보안 예제: 안전 덧셈 입력값

중간에 오버플로우가 발생하지 않게 덧셈을 하기 위해서 필요한 정보는?

---

### 보안 예제: 안전 덧셈 로직

입력 값 `x`와 `y`가 양수로 주어졌을 때, `x + y`가 오버플로우 되지 않는지 확인하는 방법은?

---

### 보안 예제: 안전 덧셈 출력 값 규칙

입력 값 `x`와 `y`가 양수로 주어졌을 때, `x + y`가 오버플로우 되었음을 나타내는 방법은?

---

### 보안 예제: 안전 덧셈 출력 값 규칙

입력 값 `x`와 `y`가 양수로 주어져야 하는데, 처리할 수 없는 값인 음수가 들어왔음을 나타내는 방법은?

---

### 보안 예제: 안전 덧셈 출력 값 규칙

입력 값 `x`와 `y`가 오버플로우 되어 있지 않아야 하는데, 처리할 수 없는 값인 범위를 넘어간 값이 들어왔음을 나타내는 방법은?

---

### 보안 예제: 안전 덧셈 정상 동작

입력 값 `x`와 `y`를 더하는 데에 문제가 없음이 확인되면 출력할 값은?

---

### 보안 예제: 안전 덧셈 함수 이름

입력 값 `x`와 `y`를 안전하게 더하는 함수의 이름으로 적절한 것은?

---

### 보안 예제: 안전 덧셈 코드

입력 값 `x`와 `y`를 안전하게 더하는 함수를 만드는 코드에서 출력 타입, 이름, 입력값을 정하는 부분은?

---

### 보안 예제: 안전 덧셈 코드

입력 값 `x`와 `y`를 안전하게 더하는 함수를 만드는 코드에서 출력 타입, 이름, 입력값을 정하는 부분은?

```c
int add_safe(int x, int y, int int_max)
{
	/* ... */
}
```

---

### 보안 예제: 안전 덧셈 함수 입력값 확인

입력 값 `x`나 `y`가 음수인 경우, 적절한 로직은?

---

### 보안 예제: 안전 덧셈 함수 입력 확인 코드

입력 값 `x`나 `y`가 음수인 경우, 에러 코드를 반환하는 코드는?

---

### 보안 예제: 안전 덧셈 함수 입력 확인 코드

입력 값 `x`와 `y`가 음수인 경우, 에러 코드를 반환하는 코드는?

```c
if (x < 0 || y < 0) {
	return -1;
}
```

---

### 보안 예제: 안전 덧셈 함수 입력값 확인

입력 값 `x`나 `y`가 오버플로우인 경우, 적절한 로직은?

---

### 보안 예제: 안전 덧셈 함수 입력 확인 코드

입력 값 `x`나 `y`가 오버플로우인 경우, 에러 코드를 반환하는 코드는?

---

### 보안 예제: 안전 덧셈 함수 입력 확인 코드

입력 값 `x`나 `y`가 오버플로우인 경우, 에러 코드를 반환하는 코드는?

```c
if (x > int_max || y > int_max) {
	return -2;
}
```

---

### 보안 예제: 안전 덧셈 오버플로우 확인 준비

`x`와 `y`를 더한 결과가 오버플로우가 될지 확인하기 위해서 필요한 정보는?

---

### 보안 예제: 안전 덧셈 오버플로우 확인 준비

`x`와 `y`를 더한 결과가 오버플로우가 될지 확인하기 위해서 `x`에 오버플로우 없이 더할 수 있는 최대값을 구하는 코드는?

---

### 보안 예제: 안전 덧셈 오버플로우 확인 준비

`x`와 `y`를 더한 결과가 오버플로우가 될지 확인하기 위해서 `x`에 오버플로우 없이 더할 수 있는 최대값을 구하는 코드는?

```c
int max_add_safe_x = int_max - x;
```

---

### 보안 예제: 안전 덧셈 오버플로우 확인 방법

`x`와 `y`를 더한 결과가 오버플로우가 될지 확인되면 수행할 적절한 행동은?

---

### 보안 예제: 안전 덧셈 오버플로우 확인 코드

`x`와 `y`를 더한 결과가 오버플로우가 되었을 때 에러 코드를 반환하는 코드는?

---

### 보안 예제: 안전 덧셈 오버플로우 확인 코드

`x`와 `y`를 더한 결과가 오버플로우가 되었을 때 에러 코드를 반환하는 코드는?

```c
if (y > max_add_safe_x) {
	return -3;
}
```

---

### 보안 예제: 안전 덧셈 정상 동작

모든 예외 상황 검사를 통과한 다음에 할 정상 동작은?

---

### 보안 예제: 안전 덧셈 정상 동작 코드

모든 예외 상황 검사를 통과한 다음에 덧셈 결과를 반환하는 코드는?

---

### 보안 예제: 안전 덧셈 정상 동작 코드

모든 예외 상황 검사를 통과한 다음에 덧셈 결과를 반환하는 코드는?

```c
return x + y;
```

---

### 보안 예제: 안전 덧셈을 `%`로 구현한다면?

나머지 연산 `%`를 한 결과의 특징은?

---

### 보안 예제: `%` 안전 덧셈 동작 범위

`%`로 범위를 정해놓은 후에 덧셈을 할 때, 덧셈 과정에서 오버플로우가 없기 위한 적절한 범위는?

---

### 보안 예제: `%` 안전 덧셈 함수 이름

`%`로 범위를 정해놓은 후에 덧셈을 하는 함수의 이름으로 적절한 것은?

---

### 보안 예제: `%` 안전 덧셈 함수 입력값

`%`로 범위를 정해놓은 후에 덧셈을 하는 함수의 입력값으로 필요한 것은?

---

### 보안 예제: `%` 안전 덧셈 함수 코드

`%`로 범위를 정해놓은 후에 덧셈을 하는 함수를 만드는 코드는?

---

### 보안 예제: `%` 안전 덧셈 함수 코드

`%`로 범위를 정해놓은 후에 덧셈을 하는 함수를 만드는 코드는?

```c
int add_safe_modular(int x, int y, int mod)
{
	x %= mod;
	y %= mod;
	return (x + y) % mod;
}
```

---
