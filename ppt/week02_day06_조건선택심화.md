### 컴프실 2주차 6일차: 조건 선택 심화

---

### 복습: 최대값을 구하는 방법

숫자 두개 중에서의 최대값은 어떻게 구하면 될까?

---

### 복습: 최대값을 구하는 코드

숫자 두개 중에서 최대값을 구하는 코드는?

---

### 복습: 정렬하는 방법

숫자 두개를 오름차순으로 어떻게 정렬할까?

---

### 복습: 정렬하는 코드

숫자 두개를 오름차순으로 정렬하는 코드는?

---

### 복습: 가위바위보를 하는 방법

가위와 바위만 낼 수 있을 때, 누가 이겼는지를
정하는 방법은?

---

### 복습: 가위바위보를 하는 코드

가위와 바위만 낼 수 있을 때, 누가 이겼는지를
정하는 코드는?

---

### 복습: 한쪽이 무한대인 구간

한쪽이 무한대인 구간을 정하기 위해 필요한 정보는?

---

### 복습: 한쪽이 무한대인 구간 코드

한쪽이 무한대인 구간을 정하는 코드는?

---

### 닫힌 구간을 나타내는 방법

양쪽이 닫힌 구간을 나타내기 위해 필요한 정보는?

---

### 조건 두개로 조건을 만드는 방법

조건 두개로부터 조건을 만드는 방법은 어떤 것들이 있을까?

---

### 조건을 연산하는 연산자의 위치

조건 두개로부터 조건을 만들 때, 연산자와 피연산자의 위치는 어떻게 정하는 것이 좋을까?

---

### 조건을 AND 하는 연산자

조건 두개가 모두 참일 때, 결과값이 참인 연산자는 어떤 기호를 쓰는 것이 좋을까?

---

### 조건을 AND 하는 연산의 특징

조건 두개가 모두 참일 때, 결과값이 참인 연산은 다른 연산과 비교해서 어떤 특징이 있을까?

---

### 조건을 AND 하는 연산 활용법

AND를 할 때 왼쪽 조건이 거짓이면, 오른쪽 조건이 계산되지 않는 특징은 코드를 짤 때 무엇이 좋을까?

---

### 조건을 OR 하는 연산의 특징

조건 두개 중 참이 있으면, 결과값이 참인 연산자는 어떤 기호를 쓰는 것이 좋을까?

---

### 조건을 OR 하는 연산의 특징

조건 두개 중 하나라도 참이면 결과값이 참인 연산은 다른 연산과 비교해서 어떤 특징이 있을까?

---

### 조건을 OR 하는 연산 활용법

OR를 할 때 왼쪽 조건이 참이면, 오른쪽 조건이 계산되지 않는 특징은 코드를 짤 때 무엇이 좋을까?

---

### `if` 문장 패턴: 예외적인 상황 걸러내기

나누기를 할 때, 나누는 수가 `0`이 아닌 경우를 `&&`나  `||`로 확인하는 대신 `if`로 확인하면 어떤 장점이 있을까?

---

### `if` 문장 패턴: 피라미드 코드

중간에 `if`로 확인할 조건들이 많아지면 코드가 어떻게 변할까?

---

### `if` 문장 패턴: 예외 조건 선처리

예외 상황일 때 함수를 종료시킬 것이면, `else if`가 필요할까?

---

### `if` 문장 패턴: 예외 조건 선처리

예외 상황일 때 함수를 종료시키면, 어떻게 피라미드 코드 문제를 해결할 수 있을까?

---

### `if` 문장 패턴: 중복 코드 패턴

`if`, `else`를 쓸 때 피해야할 패턴에 무엇이 있을까?

---

### 이동 문법의 필요성

예외 상황일 때 함수를 종료시키려고 하는데, 함수를 종료하기 전에 공통적으로 해야할 일이 있을때 어떤 문법이 있으면 좋을까?

---

### 이동 문법 키워드

이동 문법을 나타내는 키워드로 무엇이 좋을까?

---

### 이동 문법 구성

이동 문법에 필요한 구성 요소는 뭐가 있을까?

---

### 라벨링 문법

문장에 이름을 붙이는 문법에는 어떤 것들이 있으면 될까?

---

### 라벨링 문법

문장에 이름을 붙이는 기호는 무엇이 좋을까?

---

### 이동 문법 패턴: 예외 조건 선처리

예외 상황일 때 함수를 종료시키려고 하는데, 함수를 종료하기 전에 공통적으로 해야할 일이 있을때 어떻게 코드로 쓸까?

---

### 이동 문법 패턴: 반복

이동 문법을 써서 어떻게 반복작업을 할 수 있을까?

---

### 이동 문법 패턴: 스파게티 코드

이동 문법을 너무 많이 쓰면 코드를 읽기 쉬울까?

---

### 보안 예제: apple의 goto fail 버그

예외 조건 선처리를 할 때, `if` 안에 `goto`를 두번 써놓고, 중괄호를 없애면 어떻게 될까?

---

### 보안 예제: 안전한 더하기. 오버플로우란

`int`가 표현할 수 있는 범위를 넘는 값은 몇가지 종류로 분류할 수 있을까?

---

### 보안 예제: 안전한 더하기. 오버플로우 원인

`int` 표현 범위 안에 있는 두 값을 더할 때, 오버플로우가 일어나는 경우는 몇 종류가 있을까?

---

### 보안 예제: 안전한 더하기. 오버플로우 예측

더하기를 하기 전에 오버플로우가 날지 어떻게 미리 알 수 있을까?

---

### 보안 예제: 안전한 더하기. 코드

오버플로우가 날 경우를 미리 `if`로 잡아서 처리하는 코드는?

---

### 보안 예제: 안전한 더하기. 조건을 구할때

오버플로우가 날 경우를 예측하기 위해서 계산하는 수식에서 오버플로우가 나는 경우는 언제일까?

---

### 보안 예제: 안전한 더하기. 최종

오버플로우가 날 경우를 예측하기 위해서 계산하는 수식에서 오버플로우가 나는 경우는 어떻게 처리할까?

---

### C 언어의 종류?

C 프로그래밍 언어를 구현한 종류는 몇개 쯤일까?

---

### C 언어의 표준

C 프로그래밍 언어에 표준이 왜 필요할까?

---

### C 언어의 표준의 내용

C 프로그래밍 언어에 표준의 내용을 어떻게 분류할 수 있을까?

---

### C 언어의 표준에서 확실히 정하는 내용

C 표준에서 무엇을 확실히 정할 수 있을까?

---

### C 언어의 표준에서 구현마다 다른 내용

C 표준에서 구현마다 다르다고 정해놓을 수 밖에 없는 내용은 무엇이 있을까?

---

### C 언어의 표준에서 정할 필요 없는 내용

C 표준에서 결과에 큰 상관이 없고 알 필요도 없어서 정하지 않아도 될만한 내용은 무엇이 있을까?

---

### C 언어의 표준에서 정하지 않은 내용

C 표준에서 아예 정하지 않은 내용은 무슨 의미일까?

---

### 코딩 가이드라인의 필요성

더 나은 코드란 무엇일까?

---

### 코딩 가이드라인

더 나은 코드를 위한 가이드라인에 무엇이 있을까?

---

### 코딩 스타일

합법적인 이름을 짓는 규칙은 어떻게 될까?

---

### 코딩 스타일

이름은 어떻게 짓는 것이 좋을까?

---

### 코딩 스타일

들여쓰기는 몇 칸 하는 것이 좋을까?

---
