# Presentation: 컴프실_4주차_10회차_배열_032426.pptx

### 컴프실 4주차 10일차  변수 묶음


---

### 복습: 함수 만들기

```c
int main(void)
{
	return 0;
}
```

함수 만들기:  

```c
타입    이름    (    입력정보    )    {    내용    }
```

반환 문장:  

```c
return    수식    ;
```


---

### 복습: 변수 만들기

```c
int main(void)
{
	int x = 0;
	return 0;
}
```

변수 만들기:  

```c
타입    이름    =    수식    ;
```


---

### 복습: 수식 문장

```c
int main(void)
{
	int x = 0;
	x = 1 + 1;
	return 0;
}
```

수식 문장:  

```c
수식    ;
```


---

### 복습: 복합 문장

```c
int main(void)
{
	int result = -1;
	{
		result = 1 + 1;
	}
	return 0;
}
```

복합 문장:  

```c
{    문장_여러개_또는_0개    }  
```


---

### 복습: 조건부 실행

```c
int main(void)
{
	int condition = 1, result = -1;
	if (condition) {
		result = 1 + 1;
	}
	return 0;
}
```

조건부 실행 문장:  

```c
if    (    수식    )    문장
```


---

### 복습: 반복

```c
int main(void)
{
	int i, result = 0;
	for (i = 1; i <= 10; i = i + 1) {
		result = result + i;
	}
	return 0;
}
```

반복 문장:  

```c
for    (    수식    ;    수식    ;    수식    )    문장
```


---

### 변수를 묶는 이유: 데이터가 많을 때

여러 변수를 하나의 이름으로 묶는 것이 왜 필요할까?


---

### 묶음 변수를 만들기 위해 무엇이 필요할까?

보통 변수를 만들때와 비교해서 묶음 변수를 
만들 때는 어떤 정보가 더 필요할까?


---

### 묶음 변수를 만드는 것과 비슷한 상황은?

보통 변수를 만들 때 이외에 묶음 변수를 만드는 것과
비슷한 상황이 있을까?


---

### 묶음 변수를 만들 때 어떤 기호를 쓸까?

보통 변수, 함수를 만들 때와 비교해서 어떤 기호를 
묶음 변수를 나타내기에 적절할까?


---

### 묶음 변수를 만들 때 크기는 어때야 할까?

크기 정보의 타입은 무엇인 것이 좋을까?


---

### 묶음 변수를 만들 때 크기가 만족할 조건?

크기 정보가 꼭 만족해야할 조건이 있을까?


---

### 묶음 변수 크기를 변수로 정하면?

크기 정보를 변수에 저장된 값으로부터 계산하면
어떨까?


---

### 묶음 변수 크기를 변수로 정해도 될까?

크기 정보를 변수에 저장된 값으로부터 계산하는
것과 관련된 실제 C 표준이 어떻게 변해왔을까?


---

### 묶음 변수를 의미하는 단어는 무엇일까?

묶음 변수는 어떤 의미를 가진 단어로 표현하는 것이
적절할까?


---

### 묶음 변수 만들기 - 배열 선언

```c
int main(void)
{
	int num;
	int nums[10];
}
```

배열 선언:  

```c
타입    이름    [    상수_수식    ]    ;
```


---

### 묶음 변수의 초기값을 나타내는 기호

배열의 초기값을 정할 때 어떤 기호로 나타내는 것이
좋을까?


---

### 묶음 변수의 초기값을 구분하는 기호

배열의 초기값이 여러개일 때 어떤 기호로 구분하는
것이 좋을까?


---

### 묶음 변수의 초기값 정하기

배열의 초기값을 정할 때 선택할 수 있는 옵션에는
어떤 것이 있을까?


---

### 묶음 변수의 초기값 조금만 정하기

배열의 초기값을 조금만 정하면 어디에 저장될까?
나머지는 어떻게 될까?


---

### 묶음 변수의 초기값 가이드 라인

배열의 초기값을 잘 정하는 방법은 뭘까?


---

### 묶음 변수 만들기 - 배열 선언

```c
int nums[10] = {0};
```

배열 선언과 초기값 하나:  

```c
타입    이름    [    상수_수식    ]    =    {    수식    }     ;
```


---

### 묶음 변수 만들기 - 배열 선언

```c
int nums[3] = {0, 1, 2};
```

배열 선언과 초기값 여러개:  

```c
타입    이름    [    상수_수식    ]    =    {    값리스트    }     ;
```


---

### 묶음 변수 만들기 - 배열 선언

```c
int nums[3] = {0, 1, 2,};
```

배열 선언과 초기값 여러개:  

```c
타입    이름    [    상수_수식    ]    =    {    값리스트    }     ;
```


---

### 묶음 변수의 상자 하나를 나타내는 방법은?

묶음 변수에서 상자 하나를 나타내기 위해 필요한
정보는 무엇일까?


---

### 묶음 변수의 상자 하나를 의미하는 숫자

묶음 변수에서 상자 하나를 나타내는 숫자를 
뭐라고 부르면 좋을까?


---

### 묶음 변수의 상자 하나를 나타내는 방법

묶음 변수의 상자 하나를 나타내기 위해 필요한
것으로는 어떤 것이 있을까?


---

### 묶음 변수의 상자 하나를 나타내는 기호

묶음 변수의 상자 하나를 나타내는 기호로 가장
적절한 것은 무엇일까?


---

### 묶음 변수의 상자 하나를 쓸 때 인덱스 위치

묶음 변수의 상자 하나를 나타낼 때 인덱스를 어디에
쓰는 것이 적절할까?


---

### 묶음 변수의 상자에 저장된 값 읽기

```c
int nums[3] = {0, 1, 2,};
int num = nums[0];
```

배열을 구성하는 변수의 값 읽기:  

```c
배열이름    [    수식    ]
```


---

### 묶음 변수의 상자 하나에 값 저장하기

묶음 변수의 상자 하나에 값을 저장할 때는 어떤
연산자를 쓰면 될까?


---

### 묶음 변수의 상자 하나에 값 저장하기

```c
int nums[3] = {0, 1, 2,};
nums[0] = 2;
```

배열을 구성하는 변수에 값 저장하기:  

```c
배열이름    [    수식    ]    =    수식    ;
```


---

### 묶음 변수의 응용

묶음 변수가 변수가 필요한 상황은 언제일까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수

유저의 번호와 비밀번호를 저장하기 위해서 묶음
변수가 몇개 필요할까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 크기

유저의 번호와 비밀번호를 저장하기 위해서 묶음
변수 두개를 만들 때, 크기를 얼마로 만드는 것이 
좋을까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 저장

처음으로 묶음 변수들에 어떤 유저의 유저 번호와
비밀번호를 저장할 때, 몇번째 상자에 저장하는 것이
좋을까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 저장2

두번째로 묶음 변수들에 어떤 유저의 유저 번호와
비밀번호를 저장할 때, 몇번째 상자에 저장하는 것이
좋을까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 저장3

세번째로 묶음 변수들에 어떤 유저의 유저 번호와
비밀번호를 저장할 때, 몇번째 상자에 저장하는 것이
좋을까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 반복

저장된 모든 유저들의 유저 번호와 비밀번호를
확인하려고 할 때 어떤 정보가 필요할까?


---

### 유저 번호, 비밀번호 예제 - size 변수

유저 정보가 총 몇개 저장되었는지에 대한 변수는
언제 어떻게 업데이트 해야할까?


---

### 유저 번호, 비밀번호 예제 - 오버플로우

유저 정보를 추가할 때 배열의 크기를 넘어가는지는
어떻게 확인할 수 있을까?


---

### 유저 번호, 비밀번호 예제 - 묶음 변수 반복

저장된 모든 유저들의 유저 번호와 비밀번호를
확인하려면 반복 문법을 어떻게 쓰면 될까?


---

### 유저 번호, 비밀번호 예제 - 유저 삭제 의미

특정 유저를 삭제한다는 것은 묶음 변수 반복의 
관점에서 봤을 때 무엇을 의미할까?


---

### 유저 번호, 비밀번호 예제 - 유저 삭제: 배열

특정 유저를 삭제하려면 묶음 변수의 값을 어떻게
바꿔야 할까?


---

### 유저 번호, 비밀번호 예제 - 유저 삭제: size

특정 유저를 삭제하려면 size 변수의 값을 어떻게
바꿔야 할까?


---

### 유저 번호, 비밀번호 예제 - 불변성

유저를 추가할 때는 제일 끝에 추가하고, 삭제할 때는
다음 유저들을 한칸씩 왼쪽으로 당기면, 어떤 특징이
항상 유지될까?


---

### 유저 번호, 비밀번호 예제 - 유저 찾기: 위치

특정 유저 번호에 해당하는 비밀번호를 확인하기 
위해서는 어떤 정보를 알아야 할까?


---

### 유저 번호, 비밀번호 예제 - 유저 찾기: 코드

특정 유저 번호에 해당하는 비밀번호를 확인하는 
반복 문법은 어떻게 만들면 될까?


---

### 유저 번호, 비밀번호 예제 - 유저 찾기: 속도

유저 번호가 오름차순으로 정렬되어 있다면 
특정 유저 번호에 해당하는 인덱스를 더 빨리 찾을 수 
있는 방법이 무엇일까?


---

### 코딩 가이드라인: 배열의 인덱스

배열의 인덱스는 배열의 범위 안에 있어야 한다.


---

### 코딩 가이드라인: 배열의 크기

배열의 크기는 상수여야 한다.


---
