## 컴프실 1주차 2일차  프로그래밍의 원리

---

### linux를 위한 windows subsystem

- 윈도우 시작 키를 누르고, powershell 입력, Windows PowerShell 관리자로 실행

---

### linux를 위한 windows subsystem

- dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
- 컴퓨터 재부팅

---

### virtual machine

- dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
- 컴퓨터 재부팅

---

### linux 설치

- wsl --install
- 아이디, 비밀번호 설정 후 exit

---

### linux에 C 개발 도구 설치

- wsl -u root
- apt-get update
- apt-get install build-essential gdb
- exit

---

### vscode 설치

- code.visualstudio.com

---

### vscode 설치

- 다운로드 받은 설치 파일 실행
- 재부팅

---

### vscode에서 wsl 사용 설정

- 확장 프로그램 설치

---

### vscode에서 wsl 사용 설정

- WSL Install
- vscode 닫기

---

### wsl에서 vscode 켜기

- wsl
- cd ~
- code .

---

### wsl에서 vscode 켜기

- Yes, I trust the authors

---

### vscode에 C 개발 환경 구축

- 확장 프로그램 설치 

---

### vscode에 C 개발 환경 구축

- C/C++ extension pack 

---

### vscode에 C 개발 환경 구축

- C-Lab AutoSubmit

---

### macOS에서 설정 방법

- 터미널에서 xcode-select --install
- vscode에서 C/C++ (Microsoft), CodeLLDB 설치
- vscode에서 C-Lab AutoSubmit 설치

---

### 지난 시간 내용 복습

- 컴퓨터는 단순화하면 CPU와 메모리 (RAM)이다.
- 메모리에는 실행 중인 프로그램의 명령어들과 데이터들이 있다. 
- 프로그래밍 언어는 인간이 이해할 수 있는 코드를 명령어와 임시 데이터들로 바꿔준다. 

---

### 프로그래밍 언어: 간단한 연산

`1 + 2`는 기계어로 어떻게 바꾸면 좋을까?

---

### 프로그래밍 언어: 연속된 연산

`1 + 2 + 3`은 기계어로 어떻게 바꾸면 좋을까?

---

### 프로그래밍 언어: 메모리의 필요성

`1 + (2 + 3)`은 기계어로 어떻게 바꾸면 좋을까?

---

### 프로그래밍 언어: 최적화

- `1 + (2 + 3)`을 추가 메모리 없이 계산할 수 있을까?

---

### 프로그래밍 언어: 메모리의 필요성

`(1 + 2) × (3 + 4)`는 기계어로 어떻게 바꾸면 좋을까?

---

### 프로그래밍 언어: 상수 최적화

`(1 + 2) × (3 + 4)`을 미리 계산해놓아도 될까?

---

### 프로그래밍 언어: 타입

`1.1 × 2.2`는 기계어로 바꾸면 어떻게 될까?

---

### 메모리 구성: 소프트웨어 관점

명령어와 데이터를 메모리 어디에 저장하는 것이 좋을까?

---

### 메모리 구성: 명령어 관점

명령어가 많아지면 어떻게 관리해야 할까?

---

### 명령어 묶음: 실행 관점

명령어들을 실행하고 있다가, 다른 명령어 묶음을 실행하려면 어떻게 해야할까?

---

### 명령어 묶음: 실행 후 복귀 관점

명령어들을 실행하고 있다가, 다른 명령어 묶음을 실행한 후에 다른 명령어 묶음 실행이 끝난 다음에는 어떻게 해야할까?

---

### 명령어 묶음: 실행 결과 관점

명령어들을 실행하고 있다가, 다른 명령어 묶음을 실행한 후에 다른 명령어 묶음 실행이 끝난 다음에 실행 결과가 잘 됐는지, 문제가 있었는지에 대한 정보를 어떻게 전달할까?

---

### 함수 : 재사용가능, 이름있는 명령어 묶음

- C 프로그래밍 언어에서는 모든 코드를 함수 단위로 관리한다.

---

### 메모리 구성: 데이터 관점

- 데이터 하나의 크기는 몇비트가 적당할까?

---

### 메모리 구성: 정수 관점

정수가 0 이상이라면 메모리에 어떻게 저장하는 것이 좋을까?

---

### 메모리 구성: 정수 부호 관점

음의 정수는 메모리에 어떻게 저장하는 것이 좋을까?

---

### 메모리 구성: 실수 관점

실수는 메모리에 어떻게 저장하는 것이 좋을까?

---

### 타입: 0과 1로 된 값을 어떻게 해석할지

C 프로그래밍 언어에서는 모든 값에 타입이 있다.

---

### 타입: 평범한 관점

가장 많이 쓰이는 데이터 타입은 무엇일까?

---

### 가장 간단한 C 프로그램

```c
int main()
{
	return 0;
}
```

---

### 주석: 프로그램에 있는 인간을 위한 메모

```c
/* C 프로그램을 실행하면 main 함수가 실행된다 */
int main()
{
        return 0;	/* 함수의 실행 결과는 0 */
                        /* 문제가 없음을 의미 */
}
```

---

### vscode에서 실습 시작하기

- 실습 시작 버튼 클릭

---

### vscode에서 실습 시작하기

- 학번 입력

---

### vscode에서 실습 시작하기

- 이름 입력

---

### vscode에서 실습 시작하기

- 실행 버튼 클릭

---

### vscode에서 실습 시작하기

- 디폴트 옵션 선택

---

### vscode에서 실습 시작하기

- 코드 0으로 종료 확인

---

### vscode에서 실습 시작하기

- 클릭해서 중단점 만들기

---

### vscode에서 실습 시작하기

- 실행하기

---

### vscode에서 실습 시작하기

- DEBUG CONSOLE창 클릭

---

### vscode에서 실습 시작하기

- `1 + 1` 입력

---

### vscode에서 실습 시작하기

- `1 + 1` 결과 확인

---

### vscode에서 실습 시작하기

- `1 - 1` 결과 확인

---

### vscode에서 실습 시작하기

- `3 * 2` 결과 확인

---

### vscode에서 실습 시작하기

- `3 / 2` 결과 확인

---

### vscode에서 실습 시작하기

- `3 % 2` 결과 확인

---

### vscode에서 실습 시작하기

- `-3 / 2` 결과 확인

---

### vscode에서 실습 시작하기

- `-3 % 2` 결과 확인

---

### vscode에서 실습 시작하기

- `1 + 2 * 3` 결과 확인

---

### vscode에서 실습 시작하기

- `(1 + 2) * 3` 결과 확인

---

### vscode에서 실습 시작하기

- `1 - 1 - 1` 결과 확인

---

### vscode에서 실습 시작하기

- 이어서 계속 실행해서 디버깅 종료

---

### 변수: 이름있는 저장 공간

- 변수에 값을 저장할 수도 있고, 변수에 저장된 값을 계산하는데 쓸 수도 있다.

---

### 변수: 값 저장하기

변수에 값을 저장하는 문법은 어떻게 정하는 것이 가장 타당할까?

---

### 변수: 값 읽기

변수의 값을 읽는 문법은 어떻게 정하는 것이 가장 타당할까?

---

### 변수: 값 업데이트 하기

변수의 값을 사용해서 새로운 값을 구하고, 다시 저장하는 문법은 어떻게 정하는 것이 가장 타당할까?

---

### 변수: 초기값

변수에 값을 저장하지 않았으면 무엇이 저장되어 있을까?

---

### 변수: 금지된 문법

변수의 값을 읽거나 저장할 때 하지 말아야 할 것은 어떤 것이 있을까?

---

### = 대입 연산자: 결과값

대입 연산의 결과는 어떤 것이 적절할까?

---

### = 대입 연산자: 결합 방향

대입 연산자가 여러개 있으면 어떤 순서로 계산하는 것이 적절할까?

---

### vscode에서 변수 써보기

- 줄바꿈하고 `int x = 0;` 입력

---

### vscode에서 변수 써보기

- 실행 버튼 클릭

---

### vscode에서 변수 써보기

- 디버그 콘솔 클릭

---

### vscode에서 변수 써보기

- `x`를 입력해서 `x`에 저장된 값 확인하기

---

### vscode에서 변수 써보기

- `x = 1`을 입력해서 `x`에 저장된 값 바꾸기

---

### vscode에서 변수 써보기

- `x` 를 입력해서 `x`에 저장된 값 확인하기

---

### vscode에서 변수 써보기

- `x + 1`을 입력해서 `x`에 저장된 값을 계산에 써보기

---

### vscode에서 변수 써보기

- `x = x + 1`을 입력해서 `x`에 저장된 값을 업데이트 하기

---

### vscode에서 변수 써보기

- `x` 를 입력해서 `x`에 저장된 값 확인하기

---

### vscode에서 변수 써보기

- 실행 재개 버튼을 눌러서 디버그 종료

---

### 실습 1: 들여쓰기의 기만

```c
int main(void)
{
        int old_permission = 6;
        int to_remove = 2;
        int new_permission;
        new_permission = old_permission - to_remove;
                new_permission = 7; 
        return 0;
}
```

---

### 실습 2: 오버플로우

```c
int main(void) 
{
        int balance = 2000000000;
        balance = balance + balance;
        return 0;
}
```

---

### 실습 3: 8진수의 함정

```c
int main(void) 
{
        int port_a = 10;
        int port_b = 010;  /* 숫자 앞에 0이 붙어있음 */
        /* 두 포트 번호의 차이를 계산 */
        int diff = port_a - port_b;
        return 0;
}
```

---

### 실습 4: 부호의 방향

```c
int main(void)
{
        int position = -10;
        int buffer_size = 3;
        /* 역방향으로 이동했을 때의 인덱스 계산 */
        int index = position % buffer_size;
        return 0;
}
```

---

### 실습 5: 단항 연산자

```c
int main(void) 
{
        int a = 5;
        /* 빼기 기호 사이에 공백이 있음 */
        int result = - -a; 
        return 0;
}
```

---

### 실습 6: 대입 연산자

```c
int main(void) 
{
        int x = 0;
        int y = 5;
        /* 대입 연산자가 연달아 등장 */
        x = y = 100;
        return 0;
}
```

---
