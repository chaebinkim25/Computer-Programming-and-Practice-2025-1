## 컴프실 4주차 12일차: 묶음변수 심화, 메모리

---

### 복습: 유저 번호와 비밀번호를 저장할 공간

유저 번호와 비밀번호를 저장하기 위해 어떤 변수가 필요할까?

---

### 복습: 유저 번호, 비밀번호 저장 공간 코드

유저 번호와 비밀번호를 저장할 공간을 만드는 코드?

---

### 복습: 유저 번호, 비밀번호 저장 공간 코드

유저 번호와 비밀번호를 저장할 공간을 만드는 코드?

```c
int user_ids[100];
int user_pws[100];
int array_size = 100;
int data_size = 0;
```

---

### 복습: 유저 번호와 비밀번호 추가하기

유저 번호와 비밀번호를 추가하면, 변수들에 저장된 값이 어떻게 업데이트 되어야 할까?

---

### 복습: 유저 번호와 비밀번호 추가하기

유저 번호와 비밀번호를 추가하는 코드는?

---

### 복습: 유저 번호와 비밀번호 추가하기

유저 번호와 비밀번호를 추가하는 코드는?

```c
if (data_size < array_size) {
	user_ids[data_size] = 100;
	user_pws[data_size] = 1234;
	data_size += 1;
}
```

---

### 복습: 유저 번호와 비밀번호 삭제하기

특정 유저를 삭제하면, 변수들에 저장된 값이 어떻게 업데이트 되어야 할까?

---

### 복습: 유저 번호와 비밀번호 삭제하기

특정 유저를 삭제하는 코드는?

---

### 복습: 유저 번호와 비밀번호 삭제하기

특정 유저를 삭제하는 코드는?

```c
int i, del = 1;
for (i = del + 1; i < data_size; i += 1) {
	user_ids[i - 1] = user_ids[i];
	user_pws[i - 1] = user_pws[i];
}
user_ids[data_size] = 0;
user_pws[data_size] = 0;
data_size -= 1;
```

---

### 복습: 유저 번호로 비밀번호 찾기

특정 유저 번호에 해당하는 비밀번호를 찾으려면 어떻게 해야 할까?

---

### 복습: 유저 번호로 비밀번호 찾기

특정 유저 번호의 비밀번호를 찾는 코드는?

---

### 복습: 유저 번호로 비밀번호 찾기

특정 유저 번호의 비밀번호를 찾는 코드는?

```c
int i, uid = 2, pw = -1;
for (i = 0; i < data_size; i += 1) {
	if (user_ids[i] == uid)
		break;
}
if (i < data_size)
	pw = user_pws[i];
```

---

### 복습: 묶음 변수를 쓰는 방법 - 배열 리스트

배열의 작은 인덱스 쪽에 데이터를 모으면 좋은 점은?

---

### 묶음 변수를 쓰는 다른 방법은?

묶음 변수를 쓰는 다른 방법으로는 어떤 방법이 있을까?

---

### 유저 번호를 인덱스로 쓴다면?

유저 번호를 인덱스로 쓰면 유저 번호와 비밀번호를 저장하는데 배열이 몇개 필요할까?

---

### 유저 번호를 인덱스로 쓰는 것의 장점

유저 번호를 인덱스로 쓰면 어떤 장점이 있을까?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 추가

유저 번호를 인덱스로 쓸 때 어떤 유저의 유저 번호와 비밀번호 데이터를 추가하는 것을 어떻게 할까?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 추가

유저 번호를 인덱스로 쓸 때 어떤 유저의 유저 번호와 비밀번호 데이터를 추가할 수 없는 케이스는 언제가 있을까?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 추가

유저 번호를 인덱스로 쓸 때 어떤 유저의 유저 번호와 비밀번호 데이터를 추가하는 코드는?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 추가

유저 번호를 인덱스로 쓸 때 어떤 유저의 유저 번호와 비밀번호 데이터를 추가하는 코드는?

```c
user_pws[uid] = pw;
```

---

### 유저 번호를 인덱스로 쓸 때, 데이터 삭제

유저 번호를 인덱스로 쓸 때, 어떤 유저의 유저 번호와 비밀번호 데이터를 삭제하는 것은 어떻게 할까?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 삭제

유저 번호를 인덱스로 쓸 때, 어떤 유저의 유저 번호와 비밀번호 데이터를 삭제하는 코드는?

---

### 유저 번호를 인덱스로 쓸 때, 데이터 삭제

유저 번호를 인덱스로 쓸 때, 어떤 유저의 유저 번호와 비밀번호 데이터를 삭제하는 코드는?

```c
user_pws[uid] = -1;
```

---

### 유저 번호를 인덱스로 쓸 때, 초기화

유저 번호를 인덱스로 쓸 때, 배열을 처음 만들어서 모든 유저가 삭제된 것과 같은 상황일 때, 어떻게 초기값을 설정하는 것이 좋을까?

---

### 유저 번호를 인덱스로 쓸 때, 초기화

유저 번호를 인덱스로 쓸 때, 배열을 만들고 초기화하는 코드는?

---

### 유저 번호를 인덱스로 쓸 때, 초기화

유저 번호를 인덱스로 쓸 때, 배열을 만들고 초기화하는 코드는?

```c
int user_pws[100];
int array_size = 100, i;
for (i = 0; i < array_size; i += 1) {
	user_pws[i] = -1;
} 
```

---

### 유저 번호를 인덱스로 쓸 때, 반복

유저 번호를 인덱스로 쓸 때, 모든 유저에 대해 반복하는 방법은?

---

### 유저 번호를 인덱스로 쓸 때, 반복

유저 번호를 인덱스로 쓸 때, 모든 유저에 대해 반복하는 코드는?

---

### 유저 번호를 인덱스로 쓸 때, 반복

유저 번호를 인덱스로 쓸 때, 모든 유저에 대해 반복하는 코드는?

```c
for (i = 0; i < array_size; i += 1) {
	if (user_pws[i] == -1)
		continue;
	/* 반복할 작업 하기 */
}
```

---

### 유저 번호를 인덱스로 쓸 때, 탐색

유저 번호를 인덱스로 쓸 때, 특정 유저 번호를 가진 유저의 비밀번호를 찾는 방법은?

---

### 유저 번호를 인덱스로 쓸 때, 탐색

유저 번호를 인덱스로 쓸 때, 특정 유저 번호를 가진 유저의 비밀번호를 찾는 코드는?

---

### 유저 번호를 인덱스로 쓸 때, 탐색

유저 번호를 인덱스로 쓸 때, 특정 유저 번호를 가진 유저의 비밀번호를 찾는 코드는?

```c
pw = user_pws[uid];
```

---

### 변수의 크기를 1 증가시키는 연산자

변수의 크기를 1 증가시키는 연산의 결과값은 무엇이 적절할까?

---

### 변수의 크기를 1 증가시키는 연산자 기호

변수의 크기를 1 증가시키는 연산자의 기호는 무엇이 적절할까?

---

### 변수의 크기를 1 증가시키는 연산자 위치

변수의 크기를 1 증가시키는 연산에서, 결과값이 원래 변수의 값에서 1 증가된 값일 때, 연산자와 피연산자의 위치는 어떤 것이 적절할까?

---

### 변수의 크기를 1 증가시키는 연산자 위치

변수의 크기를 1 증가시키는 연산에서, 결과값이 원래 변수의 값일 때, 연산자와 피연산자의 위치는 어떤 것이 적절할까?

---

### 변수의 크기를 1 감소시키는 연산자 기호

변수의 크기를 1 감소시키는 연산자의 기호는 무엇이 적절할까? 문법은 어떨까?

---

### 배열을 쓰는 또 다른 방법

숫자들을 여러개 배열에 저장해놓고 필요할 때마다 하나씩 꺼내오는 방법에는 어떤 것이 있을까?

---

### 최신 데이터만 배열에서 꺼내기 - 정보

최신 데이터만 배열에서 꺼내기 위해서 필요한 정보는 무엇이 있을까?

---

### 최신 데이터만 배열에서 꺼내기 - 추가

최신 데이터만 배열에서 꺼내기로 할때, 배열에 데이터를 추가하기 위해 어떤 변수를 업데이트 할까?

---

### 최신 데이터만 배열에서 꺼내기 - 추가

최신 데이터만 배열에서 꺼내기로 할때, 배열에 데이터를 추가하는 코드는?

---

### 최신 데이터만 배열에서 꺼내기 - 추가

최신 데이터만 배열에서 꺼내기로 할때, 배열에 데이터를 추가하는 코드는?

```c
arr[data_size++] = new_data_add;
```

---

### 최신 데이터만 배열에서 꺼내기 - 꺼내기

최신 데이터만 배열에서 꺼내기로 할때, 배열에서 데이터를 꺼내기 위해 어떤 변수를 업데이트 할까?

---

### 최신 데이터만 배열에서 꺼내기 - 꺼내기

최신 데이터만 배열에서 꺼내기로 할때, 배열에서 데이터를 꺼내는 코드는?

---

### 최신 데이터만 배열에서 꺼내기 - 꺼내기

최신 데이터만 배열에서 꺼내기로 할때, 배열에서 데이터를 꺼내는 코드는?

```c
data_get = arr[--data_size];
```

---

### 배열 예제: 피보나치 수열이 저장된 배열

10번째 피보나치 숫자까지의 피보나치 수열을 배열에 저장하려고 할 때, 어떤 순서로 계산을 할까?

---

### 배열 예제: 피보나치 수열이 저장된 배열

10번째 피보나치 숫자까지의 피보나치 수열을 배열에 저장하려고 할 때, 필요한 배열의 크기는?

---

### 배열 예제: 피보나치 수열이 저장된 배열

10번째 피보나치 숫자까지의 피보나치 수열을 배열에 저장하려고 할 때, 계산되지 않은 칸을 확실히 표현하려면, 적절한 배열의 초기값은?

---

### 배열 예제: 피보나치 수열이 저장된 배열

10번째 피보나치 숫자까지의 피보나치 수열을 저장할 배열을 만드는 코드는?

---

### 배열 예제: 피보나치 수열이 저장된 배열

10번째 피보나치 숫자까지의 피보나치 수열을 저장할 배열을 만드는 코드는?

```c
int fibs[10], i;
for (i = 0; i < 10; i++) {
	fibs[i] = -1;
}
```

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자들을 계산하기 위해서 배열에 직접 저장해줘야만 하는 값들의 개수는?

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자 두개를 배열에 저장하는 코드는?

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자 두개를 배열에 저장하는 코드는?

```c
fibs[0] = 0;
fibs[1] = 1;
```

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자들을 계산하는 방법은?

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자들을 계산하는 코드는?

---

### 배열 예제: 피보나치 수열이 저장된 배열

피보나치 숫자들을 계산하는 코드는?

```c
for (i = 2; i < 10; i++) {
	fibs[i] = fibs[i - 1] + fibs[i - 2];
}
```

---

### 메모리: 배열이 저장되는 방식

C 표준에서 보장하는 배열이 메모리에 저장되는 방식은?

---

### 메모리: 블록과 작업대

블록에서 만든 변수가 유지되는 기간은?

---

### 블록과 변수 이름이 유효한 범위

블록에서 만든 변수의 이름이 유효한 범위는?

---

### 배열 예제: 피보나치 수열 top-down 방식

피보나치 숫자를 계산한 bottom-up 방식과 반대로 처리할 인덱스를 정하는 top-down 방식의 특징은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 현재 하려는 인덱스를 계산할 수 있는 상황인지, 계산할 수 없는 상황인지 판단하는 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 현재 하려는 인덱스를 계산할 수 있는 상황인지, 계산할 수 없는 상황인지 판단하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 현재 하려는 인덱스를 계산할 수 있는 상황인지, 계산할 수 없는 상황인지 판단하는 코드는?

```c
if (fibs[i - 1] == -1) { /* 계산 불가 */ }
if (fibs[i - 2] == -1) { /* 계산 불가 */ }
if (fibs[i - 1] > -1 && fibs[i - 2] > -1){
	/* 계산 가능 */
}
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 현재 하려는 인덱스를 계산할 수 없는 상황이라면, 나중에 다시 시도해보기위해 저장해야 할 정보는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스들을 저장하기에 적절한 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스들을 배열에 저장하고, 가장 최근에 추가된 인덱스만 꺼내오기로 할 때, 필요한 정보는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스들을 저장할 배열을 만드는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스들을 저장할 배열을 만드는 코드는?

```c
int to_do[10];
int top = 0;
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에 추가하는 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에 추가하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에 추가하는 코드는?

```c
to_do[top++] = 9;
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 더이상 시도할 인덱스가 없는지 확인하는 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 더이상 시도할 인덱스가 없는지 확인하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 더이상 시도할 인덱스가 없는지 확인하는 코드는?

```c
top == 0
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 필요한 모든 것을 계산할 때까지 반복하는 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 필요한 모든 것을 계산할 때까지 반복하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 필요한 모든 것을 계산할 때까지 반복하는 코드는?

```c
for (;;) {
	if (top == 0) break;
	/* ... */
}
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에서 꺼내오는 방법은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에서 꺼내오는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 배열에서 꺼내오는 코드는?

```c
for(;;) {
	/* ... */
	i = to_do[--top];
	/* ... */
}
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 당장 계산할 수 없다면 해야할 일은?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 당장 계산할 수 없다면 배열에 계산해야할 인덱스들을 저장하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 당장 계산할 수 없다면 배열에 계산해야할 인덱스들을 저장하는 코드는?

```c
for(;;) {
	/* ... */
	if (fibs[i - 1] == -1) {
		to_do[top++] = i;
		to_do[top++] = i - 1;
		continue;
	}
	/* ... */
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 당장 계산할 수 없다면 배열에 계산해야할 인덱스들을 저장하는 코드는?

```c
for(;;) {
	/* ... */
	if (fibs[i - 2] == -1) {
		to_do[top++] = i;
		to_do[top++] = i - 2;
		continue;
	}
	/* ... */
```

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 계산할 수 있을 때, 계산하는 코드는?

---

### 배열 예제: 피보나치 수열 top-down 방식

top-down 방식으로 진행할 때, 시도할 인덱스를 계산할 수 있을 때, 계산하는 코드는?

```c
for(;;) {
	/* ... */
	fibs[i] = fibs[i - 1] + fibs[i - 2];
}
```

---
