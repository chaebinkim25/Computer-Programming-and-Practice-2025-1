### 컴프실 5주차 14일차: 주소변수 실습

---

### 복습: 배열 리스트 만들기

```c
int arr[5] = {0};
int arr_size = 5;
int length = 0;
```

---

### 복습: 배열 리스트 초기화

```c
/* -1로 묶음 변수의 상자들을 초기화하기 */
for (int i = 0; i < arr_size; i++) {
	arr[i] = -1
}
```

---

### 복습: 배열 리스트 끝에 데이터 추가하기

```c
int new_data = 10;
arr[length++] = new_data;
new_data = 20;
arr[length++] = new_data;
new_data = 30;
arr[length++] = new_data;
```

---

### 복습: 배열 리스트 끝에서 데이터 삭제하기

```c
int old_data = arr[--length++];
```

---

### 복습: 배열 리스트 가운데 데이터 삭제하기

```c
int pos = 0;
old_data = arr[pos];
for (int i = pos + 1; i < length; i++) {
	arr[i - 1] = arr[i];
}
length--;
```

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 필요한 정보는 무엇이 있을까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 필요한 정보를 코드로 만들면?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 필요한 정보를 코드로 만들면?

```c
int pos = 1;
new_data = 25;
```

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터가 어떻게 이동해야 할까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위해서 반복을 어디서부터 어디까지 해야할까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위한 `for`의 시작 수식에서, 루프 변수의 초기값을 무엇으로 할까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위한 `for`의 조건 수식에서, 반복을 계속 할 조건을 무엇으로 할까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위한 `for`의 변화 수식에서, 반복할 때마다 루프 변수를 어떻게 바꿀까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위한 `for`를 코드로 쓰면?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동하기 위한 `for`를 코드로 쓰면?

```c
for (int i = length - 1; i >= pos; i++) {
	arr[i + 1] = arr[i];
}
```

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 기존 데이터를 이동한 다음에는 어떤 상황이 될까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 새로운 데이터를 어디에 저장하면 될까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 새로운 데이터를 저장하는 코드는?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 새로운 데이터를 저장하는 코드는?

```c
arr[pos] = new_data;
```

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 저장된 데이터 개수는 어떻게 변할까?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 저장된 데이터 개수를 바꾸는 코드는?

---

### 실습1: 배열 리스트 가운데에 데이터 추가

배열 리스트의 특정 인덱스에 데이터를 추가할 때, 저장된 데이터 개수를 바꾸는 코드는?

```c
length++;
```

---

### 실습1: 배열 리스트 가운데에 데이터 추가

```c
int pos = 1;
new_data = 25;
for (int i = length - 1; i >= pos; i++) {
	arr[i + 1] = arr[i];
}
arr[pos] = new_data;
length++;
```

---

### 실습 2: 주소를 저장하는 변수 써보기

주소를 저장하는 변수는 어떻게 만들까?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소를 저장하는 변수에 무엇을 저장하면 좋을까?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소를 저장하는 변수를 만들고, 변수의 주소를 저장하는 코드는?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소를 저장하는 변수를 만들고, 변수의 주소를 저장하는 코드는?

```c
int x = 0;
int *ptr = &x;
```

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 찾아가서 값을 어떻게 저장할까?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 찾아가서 값을 저장하는 코드는?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 찾아가서 값을 저장하는 코드는?

```c
*ptr = 5;
```

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 저장된 값을 어떻게 읽을까?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 저장된 값을 읽어서 변수에 저장하는 코드는?

---

### 실습 2: 주소를 저장하는 변수 써보기

주소에 저장된 값을 읽어서 변수에 저장하는 코드는?

```c
int y = *ptr;
```

---

### 실습 2: 주소를 저장하는 변수 써보기

```c
int x = 0;
int *ptr = &x;
*ptr = 5;
int y = *ptr;
```

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수를 구성하는 각 상자도 주소를 쓸 수 있을까?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수의 0번째 상자의 주소를 저장하는 코드는?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수의 0번째 상자의 주소를 저장하는 코드는?

```c
int arr[5] = {10, 20, 30, 40, 50};
int *ptr = &arr[0];
```

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수의 0번째 상자의 주소를 찾아가서 저장된 값을 바꾸는 코드는?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수의 0번째 상자의 주소를 찾아가서 저장된 값을 바꾸는 코드는?

```c
*ptr = 60;
```

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수의 값은 무엇이 되는 것이 적절할까?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수를 주소 변수에 저장하고, 그 주소에 저장된 값을 바꾸는 코드는?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

묶음 변수를 주소 변수에 저장하고, 그 주소에 저장된 값을 바꾸는 코드는?

```c
ptr = arr;
*ptr = 70;
```

---

### 실습 3: 묶음 변수에 속한 상자의 주소

인덱스 연산자 `[]`를 쓸 때 배열 이름은 뭐가 될까?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

주소로도 인덱스 연산자 `[]`를 쓸 수 있을까?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

주소로 인덱스 연산자 `[]`를 쓰는 코드는?

---

### 실습 3: 묶음 변수에 속한 상자의 주소

주소로 인덱스 연산자 `[]`를 쓰는 코드는?

```c
ptr[1] = 80;
```

---

### 실습 3: 묶음 변수에 속한 상자의 주소

```c
int arr[5] = {10, 20, 30, 40, 50};
int *ptr = &arr[0];
*ptr = 60;
ptr = arr;
*ptr = 70;
ptr[1] = 80;
```

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소가 `ptr_arr`에 저장되어 있다고 가정하면, 배열의 `1`번째 상자의 값을 어떻게 읽을 수 있을까?

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소를 저장하는 변수를 만드는 코드는?

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소를 저장하는 변수를 만드는 코드는?

```c
int (*arr_ptr)[5] = &arr;
```

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소에 찾아가서 두번째 상자에 가서 값을 저장하는 코드는?

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소에 찾아가서 두번째 상자에 가서 값을 저장하는 코드는?

```c
(*arr_ptr)[2] = 90;
```

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소를 찾아갈 때 `*` 대신 `[0]`을 쓰면 어떨까? 

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소를 찾아갈 때 `*` 대신 `[0]`을 쓰는 코드는?

---

### 실습 4: 묶음 변수 자체의 주소

배열의 주소를 찾아갈 때 `*` 대신 `[0]`을 쓰는 코드는?

```c
arr_ptr[0][3] = 100;
```

---

### 실습 4: 묶음 변수 자체의 주소

```c
int (*arr_ptr)[5] = &arr;
(*arr_ptr)[2] = 90;
arr_ptr[0][3] = 100;
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열은 어떻게 만드는 것이 좋을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열을 만드는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열을 만드는 코드는?

```c
int arr2d[3][5] = {0};
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 두번 쓰면 무엇을 얻을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에서 `0`번째 일차원 배열의 `0`번째 상자의 값을 읽는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에서 0번째 일차원 배열의 0번째 상자의 값을 읽는 코드는?

```c
x = arr2d[0][0];
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에서 `0`번째 일차원 배열의 `0`번째 상자에 값을 저장하는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에서 `0`번째 일차원 배열의 `0`번째 상자에 값을 저장하는 코드는?

```c
arr2d[0][0] = 110;
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 쓰면 무엇을 얻을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열은 어디에 저장할 수 있을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열을 저장하는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열을 저장하는 코드는?

```c
ptr = arr2d[0];
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열의 주소는 어디에 저장할 수 있을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열의 주소를 저장하는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열에 인덱스 연산자 `[]`를 한번 써서 얻은 일차원 배열의 주소를 저장하는 코드는?

```c
arr_ptr = &arr2d[0];
```

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열은 어디에 저장할 수 있을까?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열을 저장하는 코드는?

---

### 실습 5: 묶음 변수의 묶음 변수

이차원 배열을 저장하는 코드는?

```c
arr_ptr = arr2d;
```

---

### 실습 5: 묶음 변수의 묶음 변수

```c
int arr2d[3][5] = {0};
x = arr2d[0][0];
arr2d[0][0] = 110;
ptr = &arr2d[0][0];
ptr = arr2d[0];
arr_ptr = &arr2d[0];
arr_ptr = arr2d;
```

---
