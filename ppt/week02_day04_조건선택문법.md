### 컴프실 2주차 4일차: 조건 선택 문법

---

### 복습: 함수 만들기

```c
int main(void)
{
	return 0;
}
```

함수 만들기:  

```c
타입    이름    (    입력정보    )    {    내용    }
```

반환 문장:  

```c
return    수식    ;
```

---

### 복습: 변수 만들기

```c
int main(void)
{
	int x = 0;
	return 0;
}
```

변수 만들기:  

```c
타입    이름    =    수식    ;
```

---

### 복습: 수식 문장

```c
int main(void)
{
	int x = 0;
	x = 1 + 1;
	return 0;
}
```

수식 문장:  

```c
수식    ;
```

---

### 조건을 보고 실행하기

조건에 따라 실행 과정을 왜 바꿔야 할까?

---

### 조건을 보고 실행하는데 필요한 것

조건에 따라 실행 과정을 바꾸는 문법에는 어떤 내용이 필요할까?

---

### 조건 실행을 의미하는 단어

조건 실행을 의미하는 단어로는 무엇이 적당할까?

---

### 조건을 나타내는 방법

실행할 조건은 어떻게 나타내면 좋을까?

---

### 실행할 내용을 쓰는 자리

실행할 내용은 어디에 쓰는 것이 좋을까?

---

### 실행할 내용을 쓰는 방법

실행할 내용은 어떻게 쓰면 좋을까?

---

### 참과 거짓의 타입은?

조건에서 참과 거짓은 어떤 타입이 좋을까?

---

### 참과 거짓의 값은?

조건에서 참과 거짓은 어떤 값이 좋을까?

---

### 조건이 참이면 실행하기: if 문장

```c
int main(void)
{
	int condition = 1, state = 0;
	if (condition) {
		state = 1;
	}
	return 0;
}
```

`if` 문장:  

```c
if    (    수식    )    문장
```

---

### 조건이 거짓이면 실행하기

조건이 거짓일 때만 실행하는 문법은 왜 필요할까?

---

### 조건이 거짓이면 실행하는데 필요한 것

조건이 거짓일 때만 실행하는 문법에는 어떤 내용이 들어가야 할까?

---

### 조건이 거짓이면 실행을 의미하는 단어

조건이 거짓일 때만 실행하는 문법은 무슨 단어로 나타내는 것이 좋을까?

---

### 조건이 거짓일 때 실행되는 문법: else 문장

```c
int main(void)
{
	int condition = 1, state = 0;
	if (condition) {
		state = 1;
	} else {
		state = 2;
	}
	return 0;
}
```

else 문장:    

```c
else    문장
```

---

### 조건을 한번 더 검사하기

조건이 거짓일 때 다른 조건을 더 확인할 필요가 있을까?

---

### 조건을 한번 더 검사하는 방법

조건이 거짓일 때 다른 조건을 더 확인할 수 있는 방법은?

---

### 조건을 한번 더 검사하는 문법: else if 문장

```c
int main(void)
{
	int cond1 = 0, cond2 = 1, state = 0;
	if (cond1) {
		state = 1;
	} else if (cond2) {
		state = 2;
	} else {
		state = 3;
	}
	return 0;
}
```

---

### 숫자 하나로 조건을 만드는 방법

숫자로 조건을 만드는 방법은?

---

### 숫자 하나로 조건을 만드는 방법

숫자 하나로부터 조건을 만드는 다른 방법은?

---

### 조건 반전시키기

조건을 반전시키는 것은 왜 필요할까?

---

### 조건 반전시키기

조건을 반전시키는 것은 무엇으로 나타낼까?

---

### 조건 반전시키기

조건을 반전시키는 연산자의 위치는 어디가 좋을까?

---

### 조건을 반전시키는 문법: ! 수식

```c
int main(void)
{
	int cond = 0, state = 0;
	if (!cond) {
		state = 1;
	} 
	return 0;
}
```

`!` 수식:    

```c
!    단항수식
```

---

### 숫자 두개로 조건을 만드는 방법

숫자 두개로부터 조건을 만들 필요가 있을까?

---

### 숫자 두개로 조건을 만드는 방법

숫자 두개로부터 조건을 만드는 방법은 어떤 것들이 있을까?

---

### 숫자 두개로 조건을 만드는 방법

숫자 두개로부터 조건을 만들 때, 연산자와 피연산자의 위치는 어떻게 정하는 것이 좋을까?

---

### 두 숫자가 같은지 비교하는 문법

두 숫자가 같은지 비교하는 연산자는 무엇이 적절할까?

---

### 같은지 비교하는 문법: `==` 수식

```c
int main(void)
{
	int x = 0, y = 0, state = 0;
	if (x == y) {
		state = 1;
	} 
	return 0;
}
```

`==` 수식:    

```c
덧셈수식
비교수식    ==    덧셈수식
```

---

### 두 숫자가 다른지 비교하는 문법

두 숫자가 다른지 비교하는 연산자는 무엇이 적절할까?

---

### 다른지 비교하는 문법: `!=` 수식

```c
int main(void)
{
	int x = 0, y = 1, state = 0;
	if (x != y) {
		state = 1;
	} 
	return 0;
}
```

```c
덧셈수식
비교수식    !=    덧셈수식
```

---

### 숫자가 작은지 비교하는 문법

숫자가 작은지 비교하는 것은 무슨 의미일까?

---

### 숫자가 작은지 비교하는 문법

왼쪽 숫자가 오른쪽 숫자보다 작은지 비교하는 것은 어떤 연산자를 써서 하면 좋을까?

---

### 작은지 비교하는 문법: `<` 수식

```c
int main(void)
{
	int x = 10, y = 20, state = 0;
	if (x < y) {
		state = 1;
	} 
	return 0;
}
```

`<` 수식:    

```c
덧셈수식
비교수식    <    덧셈수식
```

---

### 숫자가 작거나 같은지 비교하는 문법

왼쪽 숫자가 오른쪽 숫자보다 작거나 같은지 비교하려면, 연산자를 무엇을 쓰는 것이 좋을까?

---

### 작거나 같은지 비교하는 문법: `<=` 수식

```c
int main(void)
{
	int x = 10, y = 20, state = 0;
	if (x <= y) {
		state = 1;
	} 
	return 0;
}
```

`<=` 수식:    

```c
덧셈수식
비교수식    <=    덧셈수식
```

---

### 숫자가 큰지 비교하는 문법

왼쪽 숫자가 오른쪽 숫자보다 큰지 비교할 때, 연산자는 무엇을 쓰면 좋을까?

---

### 큰지 비교하는 문법: `>` 수식

```c
int main(void)
{
	int x = 20, y = 10, state = 0;
	if (x > y) {
		state = 1;
	} 
	return 0;
}
```

`>` 수식:    

```c
덧셈수식
비교수식    >    덧셈수식
```

---

### 숫자가 큰거나 같은지 비교하는 문법

왼쪽 숫자가 오른쪽 숫자보다 크거나 같은지 비교하려면, 연산자는 무엇을 쓰면 좋을까?

---

### 크거나 같은지 비교하는 문법: `>=` 수식

```c
int main(void)
{
	int x = 20, y = 10, state = 0;
	if (x >= y) {
		state = 1;
	} 
	return 0;
}
```

`>=` 수식:    

```c
덧셈수식
비교수식    >=    덧셈수식
```

---

### 숫자 두개를 비교하는 수식의 우선순위

숫자 두개를 비교하는 수식은 산술 수식보다 우선 순위가 높을까?

---

### 숫자 두개를 비교하는 수식의 우선순위

숫자 두개를 비교하는 수식은 대입 수식보다 우선 순위가 높을까?

---

### `!` 수식의 우선순위

조건을 반전시키는 `!` 수식의 우선순위는 어떤 연산자 우선순위와 같을까?

---

### 코딩 스타일

줄바꿈과 띄어쓰기를 최소한으로 해서 합법적인 C 프로그램 코드를 쓰면 어떻게 보일까?

---

### 코딩 스타일

줄바꿈은 언제 하는 것이 좋을까?

---

### 코딩 스타일

띄어쓰기는 언제 하는 것이 좋을까?

---

### 코딩 스타일

들여쓰기는 언제 하는 것이 좋을까?

---

### 코딩 스타일

들여쓰기는 몇칸을 하는 것이 좋을까?

---

### 코딩 스타일

합법적인 이름을 짓는 규칙은 어떻게 될까?

---

### 코딩 스타일

이름은 어떻게 짓는 것이 좋을까?

---
