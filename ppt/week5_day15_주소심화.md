### 컴프실 5주차 15일차: 주소 심화

---

### 복습: 컴퓨터가 할 수 있는 일의 최소 단위

CPU에게 내릴 수 있는 명령 하나에는 무엇이 있을까?

---

### 복습: 컴퓨터가 작업하는 데이터의 단위

CPU에게 내릴 수 있는 명령 하나는 얼마 만큼의 데이터를 처리할까?

---

### 복습: 수식에서의 변수를 쓰는 목적

수식에서 변수를 쓰는 목적은 무엇일까?

---

### 복습: 수식에서 변수의 의미

수식에서 변수를 쓰면 무엇을 의미하게 될까?

---

### 복습: 변수를 저장하는 방법

변수는 어디에 저장하면 될까?

---

### 복습: 수식에서의 묶음 변수를 쓰는 목적

수식에서 묶음 변수를 쓰는 목적은 무엇일까?

---

### 복습: 수식에서의 묶음 변수의 의미

수식에서 묶음 변수를 쓰면 무엇을 의미하게 될까?

---

### 복습: 묶음 변수를 저장하는 방법

묶음 변수는 어디에 저장할까?

---

### 복습: 주소에 인덱스 연산자 `[]` 적용하기

묶음 변수 이름에 `[]`를 한 것과, 주소에 `[]`를 한것은 같을까?

---

### 복습: `[]`와 `*` 비교하기

주소에 `[]`를 한 것과 `*`을 한 것이 같을 수 있을까?

---

### 묶음 변수에서 다음 상자 주소 구하기

묶음 변수의 어떤 상자 주소를 가지고 있을 때, 다음 상자의 주소는 어떻게 구하게 만들면 좋을까?

---

### `[1]`과 `*` 비교하기

묶음 변수의 어떤 상자 주소 `p`를 가지고 있을 때, `p[1]`이 `*(          )`와 같기 위해서 괄호 안에 무엇이 들어가야 할까?

---

### `[2]`와 `*` 비교하기

묶음 변수의 어떤 상자 주소 `p`를 가지고 있을 때, `p[2]`가 `*(          )`와 같기 위해서 괄호 안에 무엇이 들어가야 할까?

---

### `[n]`과 `*` 비교하기

묶음 변수의 어떤 상자 주소 `p`를 가지고 있을 때, `p[n]`이 `*(          )`와 같기 위해서 괄호 안에 무엇이 들어가야 할까?

---

### 주소를 루프 변수로 쓰기

묶음 변수의 모든 상자에 대해서 반복을 하려고 할 때, 루프 변수를 주소로 쓴다면?

---

### 주소를 루프 변수로 쓰기: 시작 주소

루프 변수를 주소로 쓰는 상황에서 시작 주소는?

---

### 주소를 루프 변수로 쓰기: 끝 주소

루프 변수를 주소로 쓰는 상황에서 끝 주소는?

---

### 주소를 루프 변수로 쓰기: 주소 업데이트

루프 변수를 주소로 쓰는 상황에서 다음 주소는?

---

### 주소를 루프 변수로 쓰기: 코드

루프 변수를 주소로 써서 반복하는 코드는?

---

### 주소를 루프 변수로 쓰기: 코드

루프 변수를 주소로 써서 반복하는 코드는?

```c
int arr[5] = {10, 20, 30, 40, 50};
int arr_size = 5;
for (int *p = arr; p < arr + 5; p++) {
	/* ... */
}
```

---

### 연산자 우선순위: `[]` vs 덧셈

배열 인덱스 연산자 `[]`와 덧셈 `+` 중 무엇이 우선순위가 높을까?

---

### 연산자 우선순위: `[]` vs 부호 연산

배열 인덱스 연산자 `[]`와 부호를 바꾸는 `-` 중 무엇이 우선순위가 높을까?

---

### 연산자 우선순위: `[]` vs 주소 연산

배열 인덱스 연산자 `[]`와 주소를 구하는 `&` 중 무엇이 우선순위가 높을까?

---

### 연산자 우선순위: `&` vs 덧셈 연산

주소를 구하는 `&`와 덧셈 연산 `+` 중 무엇이 우선순위가 높을까?

---

### 연산자 우선순위: `*` vs 덧셈 연산

주소에 저장된 값을 구하는 `*`과 덧셈 `+` 중 무엇이 우선순위가 높을까?

---

### 보안 예제: 버퍼 오버플로우

묶음 변수의 범위를 벗어난 주소에 값을 저장한다면?

---

### 보안 예제: 버퍼 오버플로우

변수들 근처에 또 어떤 정보가 저장되어 있을까?

---
