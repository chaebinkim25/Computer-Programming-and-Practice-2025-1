# Presentation: 컴프실_6주차_18회차_주소 심화_040926.pptx

### 컴프실 6주차 16일차 - 코드 묶음 심화

---

### 복습: 프로그램이란 무엇인가?

프로그램은 무엇으로 이루어져 있을까?

---

### 복습: 코드는 무엇인가?

코드는 무엇으로 이루어져 있을까?

---

### 복습: 코드가 실행되는 과정은?

컴퓨터가 코드 하나를 실행하는 것은 어떤 과정을 거쳐서 이뤄져야 할까?

---

### 복습: 프로그램이 실행되는 과정은?

코드 하나의 실행이 끝난 다음에 벌어질 일은 무엇일까?

---

### 복습: 다음 코드가 저장된 곳은?

다음 코드는 어디에 저장되어 있는 것이 좋을까?

---

### 복습: 다음 코드를 불러오는데 필요한 것

컴퓨터가 다음 코드를 저장된 곳에서 불러오려면 어떤 정보가 필요할까?

---

### 복습: 다음 코드의 주소는 어디에 저장할까

다음 코드가 저장되어 있는 주소는 컴퓨터의 어디에 저장하는 것이 적절할까?

---

### 복습: `if` 문장에서 다음 코드는 무엇일까?

`if` 문장을 코드로 바꿨을 때, 다음 코드는 어떻게 정하는 것이 적절할까?

---

### 복습: `for` 문장에서 다음 코드는 무엇일까?

`for` 문장을 코드로 바꿨을 때, 다음 코드는 어떻게 정하는 것이 적절할까?

---

### 복습: 코드 묶음이 메모리에 저장되는 방법

코드 묶음은 메모리에 어떻게 저장되어 있을까?

---

### 코드 묶음을 실행하는 문장에서 다음 코드

코드 묶음을 실행하는 상황에서는 다음 코드가 어떻게 정해지는 것이 적절할까?

---

### 복습: 변수 만들기 - 메모리 관점

변수를 만든다는 것은 메모리 관점에서 봤을 때, 어떻게 한다는 것일까?

---

### 복습: 블록 안에 변수 만들기 - 메모리 관점

블록 안에 변수를 만든다는 것은 메모리 관점에서 어떻게 한다는 것일까?

---

### 복습: 블록 안에 변수 만들기 - 이름 관리

블록 안에 변수를 만든다는 것은 이름을 관리하는 관점에서 봤을 때, 어떻게 한다는 것일까?

---

### 복습: 블록 안에서 변수 이름 - 바깥 변수

블록 안에서 블록 바깥의 변수 이름을 쓸 수 있게 해주는 것이 적절할까?

---

### 복습: 블록 vs 코드 묶음 실행하기

블록에 진입하는 것과 코드 묶음이 실행되는 것에는 어떤 차이점이 있을까?

---

### 코드 묶음 실행시 변수 만들기 - 이름 관리

코드 묶음이 실행될 때 변수를 만든다는 것은 이름을 관리하는 관점에서 봤을 때, 어떻게 한다는 것일까?

---

### 코드 묶음 실행시 변수 만들기 - 중복 이름

코드 묶음을 실행할 때, 이전에 실행하던 코드 묶음 안에서 만든 이름을 쓰게 해주는 것이 좋을까?

---

### 코드 묶음 실행하기 - 메모리 관점

코드 묶음을 실행한다는 것은 메모리 관점에서 어떻게 하는 것으로 정하면 좋을까?

---

### 코드 묶음과 메모리 - 다음에 실행할 코드

코드 묶음을 실행할 때, 다음에 실행할 코드는 어디에 저장을 하는 것이 좋을까?

---

### 코드 묶음과 메모리 - 입력값 저장 시점

코드 묶음이 실행될 때, 전달할 입력값은 누가 만들어서 어디에 저장하는 것이 좋을까?

---

### 코드 묶음과 메모리 - 메모리 범위

코드 묶음이 실행될 때, 그 동안에만 유효한 메모리의 범위는 어디까지일까?

---

### 코드 묶음과 메모리 - 바깥 메모리 접근

코드 묶음이 실행될 때, 해당하는 메모리의 바깥에 접근하기 위해서는 어떤 정보가 필요할까?

---

### 주소를 코드 묶음의 입력값으로 쓰기

코드 묶음의 입력값으로 주소가 쓰이면 어떤 현상이 발생할까?

---

### 주소를 코드 묶음의 입력값으로 쓰는 목적

주소를 입력값으로 쓰는 코드 묶음에서는 어떤 작업을 할 수 있을까?

---

### 주소를 코드 묶음의 입력값으로 쓰는 코드

주소로 코드 묶음의 입력값으로 쓰는 코드는?

---

### 주소를 코드 묶음의 입력값으로 쓰는 코드

주소로 코드 묶음의 입력값으로 쓰는 코드는?

```c
int save_zero(int *x)
{
	*x = 0;
	return 0;
}
```

---

### 출력값이 없는 코드 묶음을 표시할 자리

코드 묶음에 출력값이 없다고 표시하기 위해서는 어디에 있는 단어를 바꿔야 할까?

---

### 출력값이 없는 코드 묶음을 표시하는 방법

코드 묶음에 출력값이 없다고 표시하기 위해서 쓸 단어는 무엇일까?

---

### 출력값이 없는 코드 묶음에서 `return`

코드 묶음에 출력값이 없다고 표시되어 있을 때, `return` 문장은 어떤 영향을 받을까?

---

### 출력값이 없는 코드 묶음에서 `return` 생략

코드 묶음에 출력값이 없다고 표시되어 있을 때, `return` 문장이 없으면 어떻게 될까?

---

### 출력값이 없는 코드 묶음에서 `return` 의미

코드 묶음에 출력값이 없다고 표시되어 있을 때, `return` 문장의미는 무엇이 될까?

---

### 출력값이 없는 코드 묶음을 표시하는 코드

출력값이 없는 코드 묶음을 만드는 코드는?

---

### 출력값이 없는 코드 묶음을 표시하는 코드

출력값이 없는 코드 묶음을 만드는 코드는?

```c
void save_zero(int *x)
{
	*x = 0;
}
```

---

### 입력값이 주소인 코드 묶음 실행하기

입력값이 주소인 코드 묶음을 실행할 때는 입력값으로 무엇을 전달할까?

---

### 입력값이 주소인 코드 묶음 실행하는 코드

입력값이 주소인 코드 묶음을 실행하는 코드는?

```c
void save_zero(int *x) {*x = 0;}
int main(void)
{
	int y;
	save_zero(&y);
	return 0;
}
```

---

### 입력값이 배열인 코드 묶음

코드 묶음의 입력값이 배열인 경우, 실제로 전달되는 값은 무엇일까?

---

### 입력값이 배열인 코드 묶음의 입력값

코드 묶음의 입력값이 배열인 경우, 추가적으로 필요한 정보가 있을까?

---

### 입력값이 배열인 코드 묶음을 만드는 코드

입력값이 배열인 코드 묶음을 만드는 코드는?

---

### 입력값이 배열인 코드 묶음을 만드는 코드

입력값이 배열인 코드 묶음을 만드는 코드는?

```c
void init_array(int *arr, int size) 
{
	for (int i = 0; i < size; i++) {
		arr[i] = 0;
	}
}
```

---

### 입력값이 배열인 코드 묶음 실행하기

입력값이 배열인 코드 묶음을 실행할 때 입력값으로 무엇을 전달하면 될까?

---

### 입력값이 배열인 코드 묶음 실행하는 코드

입력값이 배열인 코드 묶음을 실행하는 코드는?

---

### 입력값이 배열인 코드 묶음 실행하는 코드

입력값이 배열인 코드 묶음을 실행하는 코드는?

```c
void init_array(int *arr, int size) {
	for (int i = 0; i < size; i++) 
		arr[i] = 0;
}
int main(void)
{
	int arr[10];
	init_array(arr, 10);
	return 0;
}
```

---

### 코드 묶음을 실행하기 위해 필요한 것

코드 묶음을 실행하기 위해서 필요한 정보는 무엇이 있을까?

---

### 코드 묶음이 자신을 실행하는 이유

자신을 실행하는 코드 묶음은 어디에 쓰일까?

---

### 코드 묶음이 자신을 실행할때 주의할 점

자신을 실행하는 코드 묶음에서 발생할 수 있는 일과 예방책은 무엇일까?

---

### 자신을 실행하는 코드 묶음을 만드는 코드

자신을 실행하는 코드 묶음을 만드는 코드는?

```c
int factorial(int n) {
	if (n < 0) return 0;
	else if (n <= 1) return 1;
	else return n * factorial(n - 1);
}
```

---

### 보안 예제: 실행 흐름 탈취

묶음 코드의 실행이 끝나고 돌아갈 주소의 값을 바꾸면?

---
